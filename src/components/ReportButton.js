import React from 'react';
import './ReportButton.css'; // Import the CSS file for styles

const ReportButton = () => {
  return (
    <button className="report-button">
      Report a Fraud?
    </button>
  );
};

export default ReportButton;
